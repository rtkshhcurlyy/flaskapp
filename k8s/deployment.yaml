apiVersion: apps/v1
kind: Deployment
metadata:
  name: flaskapp
  labels:
    app: flaskapp
    tier: frontend

spec:
  replicas: 3

  selector:
    matchLabels:
      app: flaskapp

  template:
    metadata:
      labels:
        app: flaskapp
        tier: frontend

    spec:
      containers:
      - name: frontend
        image: 410003306855.dkr.ecr.sa-east-1.amazonaws.com/rtksh:latest
        ports:
        - containerPort: 5000
